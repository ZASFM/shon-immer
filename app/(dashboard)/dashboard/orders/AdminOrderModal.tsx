import { Order } from "@/app/(user)/user/orders/UserOrders";

type AdminOrderModalProps={
   order:Order
}

const AdminOrderModal=({order}:AdminOrderModalProps)=>{
   return (
      <div>modal</div>
   )
}

export default AdminOrderModal;