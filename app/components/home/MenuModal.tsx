import { MenuTypes } from "@/types";

type MenuModalProps={
   menu:MenuTypes
}

const MenuModal=({menu}:MenuModalProps)=>{
   return (
      <div>
         MenuModal
      </div>
   )
}

export default MenuModal;